apiVersion: v1
kind: Service
metadata:
  name: atlas-demo-api
  namespace: atlas-foundations
  labels:
    app.kubernetes.io/name: atlas-demo-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: stack-atlas
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: atlas-demo-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
